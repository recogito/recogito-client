import type { OpenSeadragonPopupProps } from '@annotorious/react';

import './Popup.css';

export const Popup = (props: OpenSeadragonPopupProps) => {

  return (
    <article>
      Hello World
    </article>
  )

}